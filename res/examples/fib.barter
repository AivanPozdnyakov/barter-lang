func fib(int x) -> int {
    int result = 0;
    if x < 2 {
        result = 1;
    }
    if x >= 2 {
        result = fib(x - 1) + fib(x - 2);
    }
    return result;
}
func main() -> int{
    return fib(30);
}