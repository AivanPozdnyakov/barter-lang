func fib(int x) -> int {
    int a = 1;
    a = a + 1;
    if x < 2 {
        return 1;
        1 + 3;
    }
    while x < 2 {
        return 1;
        1 + 3;
    }
    return fib(x - 1) + fib(x - 2);
    fib(2, 3);
    fib(2);
}